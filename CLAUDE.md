# Project instructions

- Do not start the dev server (`npm run dev` or similar) — the user runs and controls it themselves.
- Do not take or generate screenshots of the app. The user verifies UI changes manually in the browser.
